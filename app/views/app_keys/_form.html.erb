<%= form_for(@app_key) do |f| %>
  <% if @app_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_key.errors.count, "error") %> prohibited this app_key from being saved:</h2>

      <ul>
      <% @app_key.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.label :app_url %>
    <%= f.url_field :app_url %>
    <%= f.submit "Add Feed Url" %>
  <% end %>
